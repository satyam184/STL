#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    int a[n];
    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }
    // for value
    //  auto itr = lower_bound(a, a + n, 5);
    //  cout << *itr;

    // for index
    // auto itr = lower_bound(a, a + n, 3)-a;
    // cout << itr;

    // for value
    //  auto itr = upper_bound(a, a + n, 3);
    //  cout << *itr;

    // for index
    // auto itr = upper_bound(a, a + n, 3)-a;
    // cout << itr;
}